# 自动化测试平台 · 文档审核报告

> 审核日期：2026-08-14
> 审核对象：主规格 v1.2 / 可执行PRD v1.0 / 问题分析与补充定义 v1.0 / 原型 HTML v1.2
> 审核方法：4 份文档交叉比对 + 与《区域影像测试用例.xls》金标准规则比对 + 与 `D:\Test` 参考源码逐条核实（函数名、行号、实际实现均经 grep/sed 验证）

---

## 〇、修复状态（2026-08-14 二次更新）

| # | 级别 | 修复内容 | 状态 |
|---|------|----------|------|
| C-1 | Critical | Screen3 补"需求章节"列（9列齐全）+ 契约注释锚点 | ✅ 已修复 |
| M-1 | Major | 主规格标题改 v1.2 + 文首补 v1.2 变更摘要 | ✅ 已修复 |
| M-2 | Major | 问题分析 §1.4 段号改正（主模块=第2段、子模块=第3段） | ✅ 已修复 |
| M-3 | Major | §5.3/§6 裁决标识列口径（功能点表=base_NN 4段，meta=3段base）+ 问题分析 §5.3 featureId 注释更新 | ✅ 已修复 |
| M-4 | Major | prepareMcpProjectSession 标注"新平台规划函数" + 引用 mcpExplorationProjection.ts | ✅ 已修复 |
| M-5 | Major | insertManualFunctionPointRow 标注"新平台规划逻辑" + 补全 D:\Test 路径前缀 | ✅ 已修复 |
| M-6 | Major | §5.2 I/O 表 manualSupplement 改为 ManualSupplement 类型 | ✅ 已修复 |
| M-7 | Major | Screen3 表上方加九列契约注释锚点 | ✅ 已修复 |
| m-1~m-14 | Minor | 全部修复（映射表补行/函数名/CaseRow补字段/会话状态/导入兼容/进度矩阵/tooltip/pill口径/错别字/meta头注释/PRD依赖表述/风险表登记/分组分隔线） | ✅ 已修复 |

---

## 一、总体结论

| 级别 | 数量 |
|------|------|
| Critical（阻断） | 1 |
| Major（重要） | 7 |
| Minor（改进） | 14 |

v1.2 的大部分修正已正确落地：测试点标识 3 段规则、Screen4 表格结构（多 tbody）、子系统切换、人工补录插入位置、P0 矩阵/7 套样例引用、§17/§18 新增章节均验证通过。**但存在 1 个阻断问题（原型功能点表缺列，违反九列冻结契约）和 2 处源码引用失真（引用了 D:\Test 中不存在的函数），需在进入 P0 开发前修正。**

---

## 二、问题汇总表

| # | 级别 | 文件 | 位置 | 问题 | 建议修正 |
|---|------|------|------|------|----------|
| C-1 | **Critical** | 原型.html | Screen3 表头（L168） | 功能点表**缺"需求章节"列**，仅 8 列（序号/测试类型/系统名称/主模块/子模块/功能点/测试点/测试点标识/操作）。违反主规格 §5.3"九列固定"与 §17.2 `FeatureColumnIndexes.requirementSection`（列 2）冻结契约 | 表头补"需求章节"列（列 2 位置），数据行补对应单元格（可留空或示例"3.2.1"） |
| M-1 | Major | 主规格.md | 标题 L1、变更头 L5 | 标题仍为"主规格 v1.1"，文首变更记录只含 v1.1；文末却是"版本 v1.2"。同一文档两处版本号矛盾 | 标题改 v1.2；文首变更块补一行 v1.2 变更摘要（与文末一致） |
| M-2 | Major | 问题分析.md | §1.4 映射表 | "主模块 → 测试点标识(**第1段**)、子模块 → (**第2段**)"错误。按 3 段规则应为：系统=第1段、**主模块=第2段、子模块=第3段**。与主规格 §6（正确）直接矛盾 | 改为"主模块→第2段、子模块→第3段" |
| M-3 | Major | 主规格.md / 原型.html / 问题分析.md | 主规格 §5.3、§6；原型 Screen3；问题分析 §5.3 CaseRow.featureId 注释 | **功能点表"测试点标识"列取值口径不一致**：主规格 §5.3 示例写"测试点标识 `QYYX_PZ_JCX`"（3 段），但原型 Screen3 该列与 D:\Test `assignFeatureTestPointIds`（`next[8] = base_NN`）实际存"3段_NN"4 段唯一值；且 `aiCaseRows.ts:119` 硬断言 `caseNo === featureId` 依赖该列为完整值。问题分析 §5.3 的 `featureId` 注释示例又是 3 段。三处口径不统一，将直接影响 P1 绑定内核实现 | 在主规格 §5.3/§6 明确：功能点表"测试点标识"列 = `base_NN`（4 段唯一，行级主键）；meta 头"测试点标识" = 3 段 base（去 `_\d+$`）；用例编号 = 所绑定功能点的完整 4 段值。同步修正问题分析 §5.3 注释 |
| M-4 | Major | 主规格.md | §5.1 行为段 | 引用 `prepareMcpProjectSession`（PRD P3-T04 同样引用）。**D:\Test 全仓 grep 不存在此函数**（仅有 `server/services/mcpExplorationProjection.ts`）。文档声明"所有契约均带源码溯源"，此引用失真 | 改为引用实际存在的函数/文件，或明确标注为"新平台规划函数（非 D:\Test 溯源）" |
| M-5 | Major | 主规格.md | §5.4 L148 | 引用 `insertManualFunctionPointRow`(:154)，**D:\Test 中不存在该函数**；且该行及同段 `templateScenarioEngine.ts:72`、`caseRows.ts:157` 未带 `D:\Test` 路径前缀（违反 v1.2 声明的"源码引用统一标注 D:\Test 路径"） | 核实真实函数名（手动插入逻辑实际散落在 `App.tsx` / `caseRows.ts`）；补全路径标注 |
| M-6 | Major | 主规格.md | §5.2 I/O Boundary 表 | 探索阶段输入 `manualSupplement | ClickPath[]`，但 §18.3 / 问题分析 §五 已扩展为 `ManualSupplement`（含 `insertPosition` + `relativeToNodeId`）。**冻结契约表未同步更新**，两处定义冲突 | §5.2 表改为 `manualSupplement | ManualSupplement`（ClickPath[] 为其子字段），保持契约表为唯一真相 |
| M-7 | Major | 原型.html | Screen3 整体 | 结合 C-1：Screen3 是九列冻结契约的唯一直观呈现，缺列 + 无"需求章节"将使 P1-T01/T02 开发者以原型为准产生错误实现 | 修 C-1 后，建议在原型该表上方加 `<!-- 九列契约：主规格 §5.3/§17.2 -->` 注释锚点 |
| m-1 | Minor | 主规格.md | §6 映射表 | 缺"测试类型、需求章节 →（不直接映射）"两行（问题分析 §1.4 有），映射表不完整 | 补两行，保持与问题分析 §1.4 对齐（修正 M-2 后） |
| m-2 | Minor | 主规格.md | §5.3 | 引用 `App.tsx:1201 buildFeatureMap`，实际函数名为 `buildFeatureMergeMap`（行号正确，名称不准） | 改为 `buildFeatureMergeMap` |
| m-3 | Minor | 主规格.md | §17.3 | CaseRow 较问题分析 §5.3 少 `reviewReason?: string` 字段（虽有"概要"声明，但字段级遗漏易被 contracts 实现漏掉） | 补齐，或注明"完整定义以问题分析 §五 为准" |
| m-4 | Minor | 主规格.md | §5.1 行为段 | 子系统接入仅写"自动捕获 URL+标题"，§18.2 已扩展为"URL+标题+会话状态+导航路径"，§5.1 未同步 | §5.1 补"（含会话状态与导航路径，见 §18.2）" |
| m-5 | Minor | 主规格.md | 全文 | 问题分析 P1-7~P1-10（金标准 R0 备注行跳过、R10-R11 跨行合并、截图行动态位置、妇幼 sheet meta 第4列布局兼容）未纳入 §5.4 导入兼容要求 | §5.4 补一条"导入兼容：多 meta 布局/备注行/跨行合并/截图行位置自适应" |
| m-6 | Minor | 原型.html | Screen1 | 缺 §18.1 要求的"工作台总览：所有子系统进度矩阵"卡片（问题分析 P2-1 亦提出） | 增加进度矩阵卡片（子系统 × 阶段 ✓/⏳/—） |
| m-7 | Minor | 原型.html | Screen3 meta 头（L164）、按钮组（L162） | ① "测试点标识 QYYX_PZ_JCX" 无 tooltip（问题分析 P1-6 要求）；② 缺"加载本轮版本/保存草稿"按钮（主规格 §5.3 按钮组 6 项，原型仅 4 项）；③ "AI 提效功能点"无 tooltip（P2-8） | 补 title 属性与缺失按钮 |
| m-8 | Minor | 原型.html | 顶栏 pill（L78） | "子系统 3/3"（当前系统）与工作台统计"子系统 5"（全项目）口径混用，易误读 | pill 改为"当前系统子系统 3/3 · 项目共 5" |
| m-9 | Minor | 原型.html | m-newproj（L339） | 错别字"默认**子统**接入"→"默认**子系统**接入" | 修正 |
| m-10 | Minor | 原型.html | Screen3 顶部 meta 头 | Screen3 的 meta 头（系统名称/测试点标识/测试人员…）在主规格中无定义，与 Screen4 用例表 meta 头易混淆 | 在主规格 §5.3 或原型中注明其为"审核上下文条（非金标准 meta）" |
| m-11 | Minor | 可执行PRD.md | P4-T04 | ① 依赖写"T01,T07"——P4 无 T07（应指 P0-T07 度量脚本）；② "5 异构样例"与主规格/PRD P2-T03 的"7 套样例"表述不一（5 应指框架类型数） | 依赖改"P0-T07"；表述改"7 套样例（覆盖 5 类框架）" |
| m-12 | Minor | 可执行PRD.md | R8 | "7 套异构样例未到位"与主规格 §12"7 套异构样例已定义"表述冲突（定义 vs 可访问环境是两回事） | R8 改为"样例系统已定义（补充定义 §七），可访问运行环境待用户提供" |
| m-13 | Minor | 主规格.md | §15 层⑤ / 全文 | 问题分析 P1-2（authAdapter 接口预留）、P2-5/P2-6（测试类型枚举、需求章节格式）、P2-10（自定义规则 JSON schema）仍未在主规格落字 | 按问题分析建议补入对应章节，或在 §12 风险表登记为待办 |
| m-14 | Minor | 原型.html | Screen3 数据 | 修复后的 `QYYX_BG_JCBG_01~03` 与 3 段规则一致 ✓；但主/子模块合并单元格跨"配置/检查室"与"报告/检查报告"两组，建议在两组之间加视觉分隔线，避免纵向合并误读 | 加分组分隔样式（纯展示改进） |

---

## 三、逐项审核详情（A–H）

### A. 测试点标识规则一致性 — 基本通过，1 Major

- ✅ 主规格 §5.3 规则 = `系统缩写_父目录缩写_子系统缩写`（3 段）+ `_NN` 子系统维度递增，与问题分析 §一.1 金标准提取一致。
- ✅ 全文 grep"全表唯一 / 拼音+_NN"：主规格、PRD 中已无残留（问题分析中出现的均为"问题描述"引用，属正常）。PRD P1-T02 已改为"系统缩写_父目录缩写_子系统缩写_NN，子系统维度递增"。
- ✅ 原型全部标识数据核对：`QYYX_PZ_JCX_01~04`、`QYYX_BG_JCBG_01~03`（Screen3）、`QYYX_PZ_JCX_01/03/04`（Screen4）、Screen5/6 引用均合法。**无 `QYYX_PZ_BG_JCBG` 类 4 段错误残留**（问题分析 P0-4 已修复）。
- ⚠️ **M-3（Major）**：功能点表"测试点标识"列到底存 3 段 base 还是 4 段 `base_NN`，三份材料口径不一（详见汇总表）。已核实 D:\Test `App.tsx:1150`：`next[8] = \`${base}_${count padded}\``，即该列为 4 段唯一值；`aiCaseRows.ts:119` 断言 `caseNo === featureId` 亦以此为前提。主规格必须显式裁决。
- ⚠️ **M-2（Major）**：问题分析 §1.4 段号标注错误（主模块=第1段/子模块=第2段），与主规格 §6（第2/第3段）矛盾。

### B. 字段映射完整性 — 通过，1 Minor

- ✅ "测试点 → 测试内容"核心映射：主规格 §5.4、§6 与问题分析 §1.4 三处一致；与 `caseFieldMapping.ts:64` `caseContentFromFeatureRow`（testPoint 优先、featureName 兜底）兼容。
- ✅ "测试点标识 → 用例编号(前缀)"：三处一致，且与 §6 双重身份表一致。
- ⚠️ m-1：主规格 §6 映射表缺"测试类型/需求章节"两行（问题分析 §1.4 有），建议补齐。

### C. 表格结构正确性 — 通过

- ✅ Screen4 结构实测：`tbody(meta 7行) → tbody(列头行，th 实现) → tbody(数据行) → 软件截图行(colspan=8)`，顺序正确，**无 thead 在 tbody 之后的问题**（问题分析 P0-3 已修复，采用多 tbody 方案）。
- ✅ meta 行 `colspan 2+6=8` 与八列对齐；列头 8 列齐全，顺序与主规格 §5.4 一致。
- ✅ CSS 中遗留的 `thead th` 规则已无匹配元素（无害）。

### D. 核心数据类型定义 — 基本通过，1 Major + 1 Minor

- ✅ ModuleNode / FeatureRow(+FeatureColumnIndexes) / MetaHeader / CaseSheet / SubsystemConfig：主规格 §17 与问题分析 §五 逐字段比对一致。
- ✅ 与 D:\Test 兼容性：`FeatureColumnIndexes` 字段名与默认索引与 `caseFieldMapping.ts:1-25` **完全一致**（含 `requirementSection: 2` —— 这正是 C-1 原型缺列的对照证据）；八列顺序/列宽 `[18,16,8,34,34,14,14,12]` 与 `testProcessWorkbook.ts` 契约一致。
- ⚠️ M-3（同 A）：`CaseRow.featureId` 语义在问题分析 §5.3 注释为 3 段，与 D:\Test 实际（4 段）冲突。
- ⚠️ m-3：主规格 §17.3 CaseRow 缺 `reviewReason?: string`。

### E. 子系统工作流设计 — 通过

- ✅ 主规格 §18 与问题分析 §三 设计一致（上下文栏/快速切换/独立状态/工作台总览）。
- ✅ 顶栏切换 JS 实测正确：`toggleSubmenu()`/`switchSub()` 已定义，菜单含 3 个子系统及阶段进度，点击外自动收起逻辑（`closest('.crumbs b')` 排除）无误。
- ✅ 人工补录插入位置：模块树右键菜单（`showTreeCtx`/`ctxManual`）→ 正确联动 `m-manual` 弹窗 radio（above/below/end），实现与 §18.3/问题分析 §3.4 一致。
- ⚠️ m-6：工作台缺"子系统进度矩阵"总览卡片（§18.1 明文要求）。
- ⚠️ M-6：§5.2 冻结 I/O 表的 `manualSupplement: ClickPath[]` 未随 §18.3 扩展更新。

### F. P0 兼容矩阵和样例系统 — 通过

- ✅ 主规格 §10 正确引用问题分析 §六（70 项：框架20+路由15+容器20+恢复15）与 §七（7 套样例），抽样核对矩阵明细项数无缺漏。
- ✅ 主规格 §12 风险表已更新为"70 项矩阵**已定义**""7 套异构样例**已定义**"，无"未定"残留。
- ⚠️ m-12：PRD R8"未到位"表述与主规格口径不统一（属定义与环境之别，建议改写）。

### G. 源码引用 — 2 Major + 2 Minor

逐条与 D:\Test 核实结果：

| 引用 | 核实结果 |
|------|----------|
| `App.tsx:1201` buildFeatureMap | 行号✓，函数实名 `buildFeatureMergeMap`（m-2） |
| `App.tsx:1150` assignFeatureTestPointIds | ✓（且"需修正"标注与实际 4 段实现相符） |
| `caseFieldMapping.ts:80/:87` | ✓（`featureIdFromFeatureRow`:80、`mapFeatureRowToCaseHeader`:87） |
| `aiCaseRows.ts:84/:119/:123` | ✓（绑定断言确在 :119-121） |
| `caseRows.ts:157` sanitize | ✓ |
| `templateScenarioEngine.ts:72` | 函数✓，但 §5.4 处未带路径（M-5） |
| `qualityGate.ts` / `testProcessWorkbook.ts` | ✓ |
| `prepareMcpProjectSession` | ✗ **不存在**（M-4） |
| `insertManualFunctionPointRow`(:154) | ✗ **不存在**，且无路径（M-5） |

### H. 交叉一致性 — 基本通过

- ✅ PRD 任务与主规格对齐：P0-T07（70项度量）、P1-T02（规则已改）、P6-T01（8 屏清单与原型一致）。
- ✅ 问题分析 P0-1~P0-9 逐条复核：主规格/原型均已修正（§5.3 规则、Screen3 数据、Screen4 结构、顶栏切换、§17、§10/§12 引用、插入位置），唯 P1-6/P2-8 tooltip、P1-4 弹窗（已改选择式 ✓）中 tooltip 未做（m-7）。
- ✅ 原型数据与文档一致：Screen3↔Screen4 编号贯通（查询=_01、新增=_03、编辑=_04）；Screen1 统计与树一致（5 子系统 = 3+2）；Screen4 样例 `\` 初始值符合 CaseRow 定义。
- ⚠️ C-1 / M-1 / m-7 / m-8 / m-9 / m-11 见汇总表。

---

## 四、逐文件结论

| 文件 | 结论 |
|------|------|
| 自动化测试平台-主规格.md | **有条件通过**。修 M-1（版本头）、M-3（标识列口径）、M-4/M-5（失真引用）、M-6（§5.2 契约同步）后可作为开发依据 |
| 可执行PRD.md | **通过**（m-11/m-12 建议顺手修） |
| 问题分析与补充定义.md | **有条件通过**。修 M-2（段号错误）、M-3 中 featureId 注释后为准 |
| 原型.html | **不通过**。必须先修 C-1（Screen3 缺"需求章节"列）；其余为体验级改进 |

## 五、修正优先级建议

1. **立即（阻断 P0/P1 开发）**：C-1 原型补列 → M-3 主规格裁决标识列口径 → M-4/M-5 清理失真源码引用。
2. **本迭代内**：M-1、M-2、M-6、M-7。
3. **择机**：全部 Minor 项（m-1~m-14）。

---

**审核人**：CodeBuddy 文档审核
**核验基础**：D:\Test 源码实grep + 4 文档全文交叉比对 + 金标准规则（问题分析 §一/§二 提取稿）
